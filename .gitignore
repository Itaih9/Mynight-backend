node_modules/
dist/

*.log
logs/

.DS_Store
.vscode/
.idea/

coverage/
*.tsbuildinfo

# Markdown files
README.md

# Testing files
*.test.ts
*.spec.ts
__tests__/
test/
tests/

# Dev environment
.env.development
.env.example
.env.*

# Uploads
uploads/

# Env files
.env
.env.local

# Scripts (local scratch), except the maintenance ones that run on the server.
# Must exclude the contents, not the dir — git won't descend into an excluded
# dir, so a negation inside one never matches.
scripts/*
!scripts/showcase-video-renditions.js
!scripts/event-video-renditions.js
!scripts/showcase-index-faces.js

# Google Drive folder metadata
desktop.ini
